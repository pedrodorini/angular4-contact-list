let config = {
    database: {
        host: "localhost",
        port: 5432,
        db: "contactdb",
        user: "postgres",
        password: "admin"
    },
    server: {
        port: "3000"
    }
}
module.exports = config
